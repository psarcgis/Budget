<?xml version="1.0" encoding="utf-8"?>

<!-- This is the circle drawable that wraps around the money remaining on the header -->

<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape = "oval">
    <size
        android:height="50dp"
        android:width="50dp"
        />
    <stroke android:color="@color/colorCircleOutlineHeader"
        android:width="2dp"
        />

</shape>